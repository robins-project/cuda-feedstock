package:
  name: feature_cuda
  version: {{ feature_cuda }}

build:
  number: 0
  string: {{ cuda_impl }}
  track_features:
    - feature_cuda_{{ cuda_impl }}

test:
    requires:
      - python 3.*

extra:
  recipe-maintainers:
    - v-turin
